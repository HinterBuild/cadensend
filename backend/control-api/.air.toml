# Config file for air
root = "."
tmp_dir = "tmp"

[build]
  bin = "/bin/control-api"
  cmd = "go build -o /bin/control-api ./cmd/server"
  exclude_dir = ["node_modules", "tmp"]
  include_ext = ["go", "tpl", "tmpl", "html"]

[run]
  cmd = "/bin/control-api server"
  bin = "/bin/control-api"
